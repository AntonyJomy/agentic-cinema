# Backend URL (no trailing slash).
# Development: set this, or leave unset to use the Vite proxy.
VITE_API_URL=http://localhost:8000

# Production builds REQUIRE VITE_API_URL. There is no localhost fallback.

# Optional: load bundled mock findings instead of an empty run (dev only).
# VITE_USE_MOCK_RUN=true
